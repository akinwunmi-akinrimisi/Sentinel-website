Your Three Actions Right Now
1. Replace src/app/globals.css
Copy the globals.css file into src/app/globals.css — overwrites the scaffold version. This gives you the full Sentinel design system: navy/electric-blue palette, all @theme tokens, grain overlay, card styles, button classes, typography scale, keyframes — all in v4 syntax.
2. Add STACK_CORRECTIONS.md to your project root
Drop it alongside CLAUDE.md. Every future Claude Code session will read it and know the exact installed versions, the v4 Tailwind differences, and the audit findings. No session will try to write tailwind.config.ts or use v3 syntax.
3. Run the skills installs inside Claude Code (commands from the table above)

After Those Three — You're Ready to Build
The start command for your first Claude Code build session:
Read CLAUDE.md, STACK_CORRECTIONS.md, and WEBSITE_CONTEXT.md fully.
Then /brainstorm the Sentinel Institute homepage before touching any code.
That session will plan the full 12-section homepage, confirm the Sentinel Certification System™ visual treatment, and break everything into executable tasks via /write-plan. What do you want to tackle first — start the build, or is there anything else to configure?